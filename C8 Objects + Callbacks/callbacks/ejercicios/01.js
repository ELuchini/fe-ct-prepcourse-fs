function invocarCallback(cb) {
   // Invoca/ejecuta el callback `cb`.
   // NOTA: no debes retornar nada.
   // Tu código:
       // Simulando una operación asincrónica que tarda un tiempo en completarse
       cb(); 
}

module.exports = invocarCallback;
